/**
 * Knowledge Compounding (OSS slice) — structured project memory captured from
 * runs + `/remember`, retrieved by relevance and injected into the agent's
 * effective instructions. See `memory-node.ts` for the model.
 */
export * from './memory-node';
export * from './memory-store';
export * from './memory-context';
