{
  "name": "@excalibur/dashboard",
  "version": "1.2.0",
  "private": true,
  "type": "module",
  "description": "Excalibur OSS web dashboard — task-first kanban + runs, embedded in the CLI and served by `excalibur serve`.",
  "scripts": {
    "build": "vite build",
    "dev": "vite",
    "typecheck": "svelte-check --tsconfig ./tsconfig.json",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@excalibur/shared": "workspace:*"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^5.0.3",
    "svelte": "^5.19.0",
    "svelte-check": "^4.1.4",
    "typescript": "^5.7.3",
    "vite": "^6.0.11",
    "vite-plugin-singlefile": "^2.1.0"
  }
}
